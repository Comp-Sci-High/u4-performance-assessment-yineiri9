<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lakers Players</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
        }
        .player {
            border: 1px solid #ccc;
            padding: 20px;
            margin: 20px auto;
            width: 300px;
            box-shadow: 2px 2px 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }
        .player img {
            width: 150px;
            height: 150px;
            border-radius: 50%;
        }
    </style>
</head>
<body>
    <h1></h1>
    <div id="players">

  <h1>LakersPlayers</h1>
        <% for(let i =0; i< players.length; i++){ %>
               <p><%= players[i].firstName%></p>
               <img src="<%= players[i].photoUrl%>p}" alt="<%= players[i].firstName%> <%= players[i].LastName%>"><br>
               <strong><%= players[i].firstName%> <%= players[i].LastName%></strong><br>
               <em>Start Year: <%= players[i].startYear%></em><br>
               <p><%= players[i].funFact%></p>
            </div>

        <%}%>
    
    </div>
   
</body>
</html>
